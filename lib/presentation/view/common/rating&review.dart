import 'package:flutter/material.dart';

class RatingandReview extends StatelessWidget {
  const RatingandReview({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        /* Content for Rating & Review */
      ],
    );
  }
}