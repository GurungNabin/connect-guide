import 'package:flutter/material.dart';

class SaavedScreen extends StatelessWidget {
  const SaavedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Saved')),
    );
  }
}
